<%= form_with(model: [:admin, @category]) do |form| %>
  <div class="field">
    <%= form.label :name, '名稱', class: 'label' %>
    <%= form.text_field :name, class: 'input is-medium' %>
  </div>

  <%= form.submit '新增分類', class: 'button is-primary is-medium' %>
<% end %>